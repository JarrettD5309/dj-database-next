import { render, screen } from "@testing-library/react";
import AddTrackForm from '../src/components/add-track-form';

describe('AddTrackForm', () => {
  test.skip('should have an input for each formInput enum', () => {
   
  });
});